<!DOCTYPE html>
<html lang="en">
  <head>
    <title>BookingWeb</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-blueGray-100">
    <main class="">
      <% if user_signed_in? %>
        <%= render "layouts/shared/sidebar" %>
        <%= render "layouts/shared/notice", notice: notice %>
        <div class="relative md:ml-64 bg-blueGray-100">
          <%= yield %>
        </div>
      <% else %>
        <div class="absolute top-0 w-full h-full bg-blueGray-800 bg-no-repeat bg-full" style="background-image: url('https://bit.ly/3DErXHg');"></div>
        <div class="min-h-screen flex items-center justify-center">
          <%= yield %>
        </div>
      <% end %>
    </main>
  </body>
</html>